{
  "name": "band-schedule-app",
  "version": "1.0.0",
  "description": "Band schedule management application with Vercel KV storage",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "@vercel/edge-config": "^1.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}

